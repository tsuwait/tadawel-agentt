# 🚀 دليل الترقية — الميزات الست

## ما الجديد

| الميزة | أين | تحتاج مفتاح Claude؟ |
|---|---|---|
| ملخص صباحي | الرئيسية + إيميل تلقائي | لا |
| مقارنة بتاسي | السهم | لا |
| تنبيهات سعرية | تنبيهات | لا |
| تقويم التوزيعات | توزيعات | لا |
| مصادر الإعلانات | السهم | لا |
| حاسبة الزكاة | زكاة | لا |

كلها تشتغل مجاناً. المفتاح يلزم فقط لتلخيص سبب الحركة آلياً.

---

## خطوة ١ — ارفع الملفات

ملفات **جديدة** (٧):
`storage.py` · `alerts.py` · `dividends.py` · `zakat.py` ·
`announcements.py` · `brief.py` · `UPGRADE_AR.md`

ملف **محدّث** (١): `app.py` — استبدل القديم

ملف **اختياري**: `.github/workflows/daily_brief.yml` للإيميل الصباحي

من سفاري: مستودعك ← `+` ← Upload files ← Commit.
Streamlit يعيد النشر تلقائياً خلال دقيقتين.

---

## خطوة ٢ — الإيميل الصباحي (اختياري)

يشتغل عبر GitHub Actions — مجاني تماماً.

**ليش نحتاجه؟** Streamlit المجاني ينام ولا يشغّل شيئاً في الخلفية.
فما يقدر يرسل لك تنبيهاً من نفسه. GitHub Actions يشتغل بجدولة
مستقلة، يفتح البيانات، ويرسل لك الملخص.

### الإعداد

**١) جهّز كلمة مرور تطبيقات من Gmail**
حسابك في Google ← الأمان ← فعّل التحقق بخطوتين ← كلمات مرور التطبيقات
← أنشئ واحدة. **لا تستخدم كلمة مرور حسابك العادية.**

**٢) أضف الأسرار في GitHub**
مستودعك ← Settings ← Secrets and variables ← Actions ← New repository secret

| الاسم | القيمة |
|---|---|
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `587` |
| `SMTP_USER` | إيميلك |
| `SMTP_PASS` | كلمة مرور التطبيقات |
| `MAIL_TO` | الإيميل المستقبِل |

**٣) فعّل Actions**
تبويب Actions في المستودع ← فعّل ← اختر «الملخص الصباحي» ←
**Run workflow** للتجربة فوراً.

يرسل تلقائياً ٨:٣٠ صباحاً بتوقيت الرياض، الأحد إلى الخميس.

> لإرساله لأبوك وأخيك: ضع إيميلاتهم في `MAIL_TO` مفصولة بفاصلة.

---

## حدود يجب أن تعرفها

**التنبيهات ليست لحظية.** تُفحص عند فتح التطبيق أو مع الملخص الصباحي.
تنبيه لحظي حقيقي يحتاج خدمة تعمل دائماً — بوت تيليجرام أقرب حل.

**مواعيد التوزيعات تقديرية.** محسوبة من نمط السنوات السابقة، لا من
إعلان رسمي. الموعد المعتمد الوحيد هو إعلان الشركة في موقع تداول.
لذلك يظهر التقدير موسوماً صراحةً في الواجهة.

**حاسبة الزكاة ليست فتوى.** المسألة فيها اجتهادات، خصوصاً في نية
الاستثمار طويل المدى. الحاسبة تعرض الطريقتين الشائعتين دون ترجيح،
وتترك لك إدخال نسبة الموجودات الزكوية. راجع جهة شرعية معتبرة.

**البيانات تنمسح عند إعادة التشغيل.** استخدم النسخ الاحتياطي في
«حالة النظام» — انسخ الكود واحتفظ به في ملاحظاتك.

---

## أفكار للخطوة القادمة

- **تخزين دائم:** استبدل `load`/`save` في `storage.py` بـ Supabase
  (مجاني). بقية المشروع لا يتغير — يتعامل مع الدوال فقط.
- **بوت تيليجرام:** يحل مشكلة التنبيه اللحظي، ويعطي أبوك واجهة
  أسهل من المتصفح.
- **مقارنة بالقطاع** لا بالسوق كله — أدق في الحكم على أداء السهم.
