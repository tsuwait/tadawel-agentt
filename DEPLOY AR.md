# 📱 تشغيل الوكيل على الأيفون

الأيفون ما يشغّل Python مباشرة. الحل: تنشر التطبيق على الإنترنت وتفتحه من سفاري
كأنه تطبيق عادي. أفضل خيار مجاني هو **Streamlit Community Cloud**.

---

## الطريقة الأولى: النشر على السحابة (الموصى بها)

### 1) ارفع الملفات على GitHub
- افتح <https://github.com> وسجّل حساب لو ما عندك
- اضغط **New repository** وسمّه مثلاً `tadawul-agent`
- خلّه **Private** (مهم — عشان تستفيد من خانة التطبيق الخاص المجانية)
- ارفع كل الملفات ما عدا ملف `.env`

> ⚠️ لا ترفع ملف `.env` أبداً. المفتاح ينحط في مكان ثاني (خطوة ٣).

### 2) اربط Streamlit
- افتح <https://share.streamlit.io> وسجّل دخول بحساب GitHub
- اضغط **Create app** ← **Yup, I have an app**
- اختر المستودع، والفرع `main`، والملف الرئيسي: `app.py`

### 3) حط الأسرار قبل ما تضغط Deploy
اضغط **Advanced settings** ← **Secrets** والصق:

```toml
ANTHROPIC_API_KEY = "sk-ant-مفتاحك"
APP_PASSWORD = "كلمة-سر-تختارها"
```

`APP_PASSWORD` يمنع أي أحد يعثر على الرابط من استهلاك رصيد الـ API حقك.
لو تركتها فاضية، التطبيق يفتح للجميع.

### 4) اضغط Deploy
خلال دقيقتين إلى خمس بيطلع لك رابط شكله:
`https://اسم-اخترته.streamlit.app`

### 5) افتحه على الأيفون واحفظه كتطبيق
- افتح الرابط في **سفاري**
- زر المشاركة (المربع مع السهم) ← **إضافة إلى الشاشة الرئيسية**
- بيصير له أيقونة مثل أي تطبيق، ويفتح بدون شريط المتصفح

---

## حدود الخطة المجانية

- ذاكرة تقريباً ١ جيجا — كافية لهذا المشروع
- التطبيق ينام بعد ١٢ ساعة بدون استخدام، وأول فتحة بعدها تاخذ ٣٠–٦٠ ثانية
- تطبيقات عامة بلا حد، وتطبيق خاص واحد فقط
- ما فيه دومين مخصص

لو ضاقت فيك: Railway أو Render أو أي VPS بدولارات بسيطة شهرياً.

---

## الطريقة الثانية: من جهازك على نفس الواي فاي (للتجربة السريعة)

لو عندك ماك أو ويندوز في البيت، وتبي تجرّب من جوالك بدون نشر:

```bash
streamlit run app.py --server.address 0.0.0.0
```

بيطبع لك سطر **Network URL** شكله `http://192.168.1.20:8501` —
افتحه من سفاري في الأيفون وأنت على نفس الشبكة.

يشتغل داخل البيت فقط، ولازم اللابتوب شغّال.

---

## الطريقة الثالثة: بدون GitHub

**Hugging Face Spaces** يخليك ترفع الملفات من المتصفح مباشرة:

1. <https://huggingface.co/spaces> ← **Create new Space**
2. اختر **Streamlit** كنوع، و **Private** للخصوصية
3. ارفع الملفات من تبويب **Files**
4. حط المفتاح في **Settings ← Variables and secrets** باسم `ANTHROPIC_API_KEY`

نفس الشي — رابط تفتحه من الجوال.

---

## بدائل لو تبيه تطبيق أصلي على الأيفون

الحل الأنظف بدون ما تبني تطبيق iOS: خلّ الوكيل يشتغل كبوت.

- **بوت تيليجرام**: أسهل خيار. مكتبة `python-telegram-bot`، تستبدل `app.py`
  بمعالج رسائل ينادي `TadawulAgent.chat()` وترجّع النص. تكلّمه من تطبيق
  تيليجرام على جوالك عادي.
- **واتساب**: عبر Twilio أو WhatsApp Business API — أعقد وفيه رسوم.
- **اختصارات آيفون (Shortcuts)**: تحوّل `agent.py` لـ API بسيط بـ FastAPI،
  وتسوي اختصار يرسل سؤالك ويعرض الجواب. ينفع تناديه بسيري.

قل لي أي وحدة تبي وأجهزها لك.
